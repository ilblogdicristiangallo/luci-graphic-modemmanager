# luci-graphic-modemmanager
LuCI app for OpenWrt: ModemManager (mmcli) UI — modem status, LTE and 5G SA/NSA band lock with clickable green badges, diagnostics. Bands are read dynamically from the modem, not hardcoded.
